// Тест: цикл while
fn sum_to(int n) -> int {
    int total = 0;
    int i = 1;
    while (i <= n) {
        total = total + i;
        i = i + 1;
    }
    return total;
}

fn main() -> int {
    int result = sum_to(5);
    return result;
}
