// Полная программа: вычисление факториала через цикл
fn factorial(int n) -> int {
    int result = 1;
    while (n > 1) {
        result *= n;
        n -= 1;
    }
    return result;
}

fn main() {
    int val = factorial(5);
}
